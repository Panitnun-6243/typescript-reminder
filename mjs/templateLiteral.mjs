// template literal
export const templateLiteral = () => {
  const str = 'Hello' + ' ' + 'Literal'
  console.log(str)

  // interpolation
  const userName = 'Tanny'
  const fullName = `Hi, Mr. ${userName}`
  console.log(fullName)
}
